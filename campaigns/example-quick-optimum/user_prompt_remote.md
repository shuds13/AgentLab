Start the run.

Read `LOGBOOK.md` and `results.jsonl` and summarise where things stand. If they are
empty, this is the first run.

This run is for locating the optimum: bracket it, then narrow it until you can state it
with an interval the readings support. The shape of the curve either side is for a
later run unless what you measure settles it along the way.

Run the readings on the compute system with `submit_job`, collected with
`get_completed_jobs`.
